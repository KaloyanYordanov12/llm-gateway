# ── Working docs (planning artifacts — NEVER commit to this repo) ──
llm-gateway-plan.md
llm-gateway-plan-v2.md
llm-gateway-preflight.md
llm-gateway-phase-0-brief.md
files.zip

# ── Maven ──
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties
.mvn/timing.properties
# Note: .mvn/wrapper/maven-wrapper.jar IS committed so CI can run ./mvnw without a global Maven.

# ── Java ──
*.class
*.log
*.jar
!.mvn/wrapper/maven-wrapper.jar
!**/maven-wrapper.jar
hs_err_pid*
replay_pid*

# ── IDE: IntelliJ IDEA ──
.idea/
*.iml
*.ipr
*.iws
out/

# ── IDE: Eclipse ──
.project
.classpath
.settings/
bin/

# ── IDE: VS Code ──
.vscode/

# ── Frontend (React/Vite dashboard) ──
frontend/node_modules/
frontend/dist/
frontend/node/

# ── OS ──
.DS_Store
Thumbs.db

# ── Env / secrets (never commit) ──
.env
*.env
!.env.example
