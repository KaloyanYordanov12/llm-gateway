/**
 * Root package of the LLM Gateway: holds the Spring Boot application entry point.
 * Feature logic lives in the sibling feature packages (proxy, auth, ratelimit,
 * provider, cache, usage, management, config, error).
 */
package dev.kaloyanyordanov.llmgateway;
