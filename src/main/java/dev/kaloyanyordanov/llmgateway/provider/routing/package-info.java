/**
 * Per-model routing: pure, config-driven resolution of a logical model to an
 * ordered list of concrete (provider, upstream-model) targets, and the executor
 * that invokes them. Adding a routing rule is configuration, not code.
 */
package dev.kaloyanyordanov.llmgateway.provider.routing;
